# 目的
面向中文开发者的 Agent Skills 集合：每个 skill 解决一类真实重复劳动，装上就能用，产出可被展示验证。

# 约束
MR: on

# 日志
- 2026-09-03 10:15：给 ci-review 做展示卡（狗粮第一件事）。cards.json 加第 9 条，内容取自 PR #8 机器人的真实评论；build_cards.py 生成 HTML，Playwright 1200x675 视口截叠放 index.html 后按 675px 切出 cards/ci-review.png（已目检：标题/双栏/红绿标注渲染正常）；README 画廊加一格并配飞轮实景说明。验证：sips 确认 1200x675，README 引用路径与文件一致。下一步：合并后观察 ci-review 对本 MR 的审查。
- 2026-09-03 23:18：先回应上轮"下一步"——查 PR #9/#11 的 ci-review 结论：均 pass、零未解决线程，反馈面干净。本次：x-article-download/SKILL.md 650 行超仓库红线（500 行），把路径 D（整账号批量）与路径 E（Article 图片）整体平移到 references/x-batch-account.md（288 行）、references/x-article-images.md（50 行），SKILL.md 瘦身到 324 行并保留路由与 Article 前置沟通警示。验证：与 git show HEAD:x-article-download/SKILL.md 逐行比对，头部/尾部/frontmatter 字节级一致，路径 D 286 行、路径 E 48 行无丢失（仅节间 --- 分隔线未平移）；wc -l 确认 324 ≤ 500；SKILL.md 引用的两个 references 文件均存在。疑虑：切分时发现 Read 工具显示行号与原始文件在 400-600 行区间有 2 行偏移，已改用内容锚点切分规避。下一步：等 ci-review 审查本 MR；pass 且无线程将自动合并（档位已 on）。
- 2026-09-03 23:50：第 0 步——PR #12 被机器人自动合并（审查 pass→pr ready→merge，全自动收割首次跑通）。本次：把手工体检固化为市场质量门禁，.github/scripts/validate_marketplace.py（纯标准库：frontmatter/行数/硬编码路径/引用存在/可执行位/索引一致性）+ .github/workflows/validate.yml（PR 与 main push 触发）；顺带修复门禁扫出的 10 处真违规（hkr-render、md2view 的 scripts 补 chmod +x）。验证：主仓跑门禁 ✓ 12 skill/13 entry 退出码 0；/tmp 拷贝注入 7 类违规（name 不匹配/超 500 行/硬编码路径/引用缺失/README 缺行/无可执行位/JSON 损坏）逐类退出码 1，恢复后复绿 0。裁决记录：~/.claude 硬编码规则收窄到 ~/.claude/skills/（repo-map/repo-tidy 引用的是用户级配置文件，属合法引用）；README 正则修正为 ./name/ 链接格式。疑虑：门禁查 SKILL.md 不查 README 的路径写法（README 是人读文档）；引用检查只认反引号包裹的 scripts|references 路径。下一步：观察本 MR 上 validate 与 ci-review 双检查并跑。
- 2026-09-03 23:59：第 0 步——PR #13 被 ci-review 判 fail：field_value() 对 `description: |` 块标量返回字面 "|"，description 缺失/≤1024 两项检查对全仓 12 个 skill 的实际格式零效力（真 bug，认可）。修复：field_value 识别 |、|-、> 等块标量指示符，读后续缩进行拼值。验证：/tmp 注入机器人原失败场景（| 格式 1200 字符 description）→ 拦截；缺 description → 拦截；单行 1100 字符 → 拦截；主仓复检 ✓ 退出码 0。已回复机器人线程「已修」并 resolve。下一步：等 ci-review 复审本修复。
- 2026-09-04 00:08：第 0 步——ci-review 复审仍 fail：field_value 重写时删掉了空内联续行回退，合法 YAML plain 多行 description（旧版通过）被误报缺 description（机器人附了新旧版同输入对比，认可）。修复：空内联恢复续行读取（plain 多行按 YAML 折叠为空格），保留遇非缩进行即停。验证：机器人原复现（| 改 plain）→ 通过；plain 多行 1200 字符/缺 description/| 格式 1200 字符三拦截场景不回归；主仓 ✓ exit 0。流程教训：上轮重写删路径时没对被删路径做回归测试，本轮矩阵补上。已回复线程并 resolve。下一步：等第三轮复审。
- 2026-09-04 00:35：第三轮复审仍 fail：field_value 还有两个合法 YAML 洞——非空内联 plain 多行只量首行、|2 显式缩进记号被当值返回（均为历史存在、落在我重写的行上）。三轮打洞后停止补丁循环，根治：frontmatter 解析改用 PyYAML（workflow 显式 pip install），覆盖全部合法格式并新增「frontmatter 不合法 YAML」检查；手写解析器退化为无 PyYAML 时的兜底，两洞也在兜底内补齐（指示符改 [\|>][+-]?[0-9]* 正则、非空内联并入续行折叠）。验证（8 场景）：非空内联多行 1200 拦截、|2+1500 拦截、tab 缩进报 YAML 不合法、含冒号引号值正确解析、plain 多行/缺 description 不回归、第一轮原复现仍通过、屏蔽 PyYAML 后主仓绿且 |2 超长仍拦、主仓终检 exit 0。已回复两条线程并 resolve。下一步：等第四轮复审。
- 2026-09-04 00:58：第四轮复审 fail，两条：先自我纠错——上轮声明「⑥ 原复现仍通过」是假通过：BSD sed 不支持 GNU 的 0,/re/ 地址，sed 静默失败没改文件，我验证的是未修改版本（撤回该声明，验证矩阵改为 python 变异+每步断言变异生效）。裁决：线程1「误报复活」不认可——含冒号空格续行的 plain 多行 YAML 语义就是 dict，skill 加载器读到的同样是坏值，拦截正确（上轮手写解析器放行恰是其 YAML 无知 bug）；但文案失真认可，改为报「description 不是字符串（YAML 解析为 dict）」。线程2 |4- 漏匹配认可，处置升级：整个删除兜底解析器，PyYAML 成为唯一路径（双路径=双行为=永久 review 面），无 PyYAML 时启动即报错提示安装。验证（python 变异+变异生效断言，8 场景）：| 改空内联报不是字符串 exit 1、|4- 1021 放行、|2 1500 拦、缺 description 拦、tab 报 YAML 不合法、非空内联 1200 拦、合法 plain 多行放行、主仓 exit 0。已回复两条线程并 resolve。下一步：等第五轮复审。
- 2026-09-04 02:16：第 0 步——PR #13 收割（第五轮 pass，0 新发现）。本次：把 6 轮飞轮实战教训写回生成器——do-something/SKILL.md「与 ci-review 配对」节新增验证声明纪律（可独立重跑的粒度；验证工具静默失败陷阱：sed 方言、变异未生效须先断言），源头是 00:58 撤回的假通过声明；CLAUDE.md §6.2 手工 grep 体检收敛到门禁脚本（原 grep -r ~/.claude 会误伤 repo-map/repo-tidy 的合法用户级引用，与门禁裁决矛盾）。验证：两文件精确替换（assert 锚点）；do-something/SKILL.md 136 行 ≤500；主仓门禁 exit 0。下一步：等 ci-review 审查（纯文档改动按规范直接 pass）。
- 2026-09-04 02:51：第 0 步——PR #14 收割。本次：给零验证历史的 git-push-guard（守护所有仓库直推闸门的 hook-only skill）做行为实测并修洞。新写 scripts/test_hook.sh（18 场景：scratch 仓库+受控 HOME，allow/ask 断言）。实测坐实 4 个放行漏斗：裸 push 带 remote 名（git push origin）、git -C <path> 前缀整体绕过、--all、--mirror；另坐实 allowlist 在 macOS 符号链接路径（/var vs /private/var）下静默失配。修复：hook 重写识别逻辑——master/main 上无显式非默认分支目标（排除 remote 名）一律 ask（覆盖 --all/--mirror/仅 remote），前缀正则统一支持 git -C 形态，allowlist 双侧 pwd -P 归一。验证：18/18 场景通过（含 4 漏斗复测、注释/空行 allowlist、未命中仍拦）；deny 理由文本完整；bash -n 双脚本通过；门禁 exit 0。疑虑：deny 语义是 ask 非硬拒，--force-with-lease=main:x 被整体当 flag 宁拦勿放（过拦可接受）；remote 名判定依赖 git remote 输出。下一步：观察本 MR 审查；后续每轮可挑一个零验证 skill 实测（md2view 自带测试套件是下一个候选）。
- 2026-09-04 03:19：第 0 步——ci-review 判 fail，三条全部认可：①分支判定取会话 cwd 而非 -C 目标仓库，跨仓库 --all/--mirror/仅remote 复活（我的测试 cwd 恰等于 -C 路径，掩盖了不一致分支）；②prefix 正则不吃引号空格路径，git -C "带 空格" push 整体绕过；③refs/heads/main 被当显式非默认目标放行。重写：判定对象改为 push 位点的目标仓库（-C 优先），多位点命令从宽整体 ask，引号路径进 path_alt，默认分支等价写法（refs/heads/、*/master、origin/main、@{u}）逐 token 判定，并新增规则：--all/--mirror 与当前分支无关、按仓库是否存在本地 master/main 判（同族新洞，feature 分支跑 --all 也会推走 master）。过程中自抓并修掉重写引入的 allowlist tr -d 吃内部空格 bug。验证：测试套件扩到 31 场景全绿（跨仓库 5、引号路径 2、等价写法 3、--all 内容判定 2、多位点 1 + 原 18 不回归）；端到端实弹——跨仓库 git -C push --all 真推，远端分支数 0；bash -n 通过；门禁 exit 0。测试套件自身曾因 mkgit 辅助函数把仓库路径混进 git 子命令而整体失效过一轮（假绿），已修。下一步：等下一轮复审。
- 2026-09-04 03:48：第三轮复审 fail，5 条全部认可，共性根因是我用行正则做 shell 命令解剖、每轮补形态机器人再找出新形态。本轮做两个结构级修正而非继续打补丁：①位点后参数区改用 bash 后缀删除（${cmd#*"$site"}），位点前 token 不再泄入（修回归：git add -A && git commit && git push 放行）；②位点提取先去前导空白再剥 -C（修回归：中段位点 cpath 垃圾串、判定退回 cwd，master 实际落远端）。③语义收紧：目标仓库在 master/main 上任何 push 一律 ask（默认分支上没有正常 push 需求，误拦只是确认一次、漏放是事故——不对称取激进侧），直接消灭 +main/HEAD/@{u} 等'等价写法清单'整个家族；feature 分支保留两类判定：refspec 指向默认分支（含 +、refs/heads 形态）与删远端默认分支（--delete/-d/--delete= 三形态，含跨分支删远端 main）。验证：套件扩到 42 场景全绿；两个回归场景实弹复测（ask + 远端分支数 0）；bash -n、门禁通过。遗留疑虑：cd 切目录后的 push 无法看到目标目录（hook 只见会话 cwd），alias 别名 push 无法识别——均为 ask 闸门设计下的已知盲区，记入 README 待人类裁决是否值得修。已回复 5 条线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 04:17：第四轮复审 fail，3 条全部认可：①无 -C 位点 sed 残留字面量 push，cwd 恰有同名文件时裸 push 静默放行（上轮重写引入）；②检测层逃逸——git 前是非空白字符（&&git、$(git)、反引号）时 prefix 不匹配，'任何 push 一律 ask'声明对这些形态为假（实跑 master 落远端）；③规则 3/5 的 token 扫描跨命令段边界，git checkout master / ls --all 的后段参数被误读为 push 参数（新误报）。修复：cpath 仅在位点含 -C 时提取（残留通道关闭）；prefix 边界放宽为非名字字符（[^[:alnum:]_-]，兼容逃逸形态又不误吞 gitx），位点边界字符剥离后重建 git 前缀；参数区在首个 |&; 处截断（多位点已整体 ask，位点后命令段不可能是 push）。验证：套件扩到 51 场景全绿（同名 push 文件作为常驻回归条件加入全部场景；逃逸形态 3 条；跨段误报 2 条；-d 两态 2 条）；$(git push) 实弹——ask + 远端 0 分支；bash -n、门禁通过。已回复 3 条线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 04:46：第 0 步——PR #15 收割（第五轮 pass，探针无新回归）。verdict 移交清单即本次：①引号 refspec（"refs/heads/main"、'main'）在 feature 分支静默放行——token 分类前先 norm（去 + 前缀与成对引号），三处分类点统一走 norm()；②git -C<path> 无空格粘连形态整体绕过——prefix 的 -C 后空格改 *，cpath 触发条件放宽为 *-C*，提取 sed 同步。验证：套件扩到 58 场景全绿（引号 4 态、粘连 2 态、空格形态不回归）；粘连形态实弹——ask + 远端分支数 0；bash -n、门禁通过。下一步：等 ci-review 审查本 MR。
- 2026-09-04 05:17：第 0 步——ci-review 判 fail：粘连族确认关闭，但引号族只关一半——+ 在引号内（"+main"）norm 剥不到、含冒号的引号形态（":main"、"HEAD:main"、HEAD:"main"）rule2 正则吃不到收尾引号、rule5 清单无冒号 glob。两条认可。修复：①norm 改为删全部引号字符（合法 refspec 不含引号，零误伤）+ 迭代剥 +；②分类清单补冒号族并收敛为 is_default_spec 函数。过程自抓一个 bash 认知 bug：case 变量展开不解析 | 交替（整值当单一 pattern），首版 $default_specs 写法全量漏匹配——被自己的套件当场拦下（15 失败），最小化复现确认后改逐 glob 判定。验证：套件扩到 67 场景全绿（引号内 + 3 态、冒号引号 5 态、非默认 refspec 防误拦 1 条）；引号 HEAD:main 实弹 ask；bash -n、门禁通过。已回复 2 条线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 05:46：第七轮复审 fail：ANSI-C 引号 $'master'（norm 残留 $）与反斜杠转义 m\aster（不删 \）在 feature 分支静默放行，实弹均落远端，含删除/强推变体。两条认可，且这轮收类而非再补形态：shell 引用/转义形态无界，norm 改白名单保留法——tr -cd 只留合法 refspec 字符集（[:alnum:]_/.:+-），其余全删后迭代剥 +；连字符等合法字符不受影响。验证：套件扩到 73 场景全绿（ANSI-C 3 态、反斜杠 2 态、连字符分支防误伤 1 条）；m\\aster 实弹 ask；bash -n、门禁通过。已回复 2 条线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 06:18：第八轮复审 fail：$'\x6d\x61\x69\x6e'（ANSI-C 十六进制转义 = main）静默放行实弹落远端——转义序列的编码文本（x、数字）本身是合法 refspec 字符，白名单 norm 挡不住，上轮'收类关闭'声明被证伪（撤回）。本轮换判定论收口：纯/杂分流——纯字面 token（只含 refspec 合法字符）才静态分类，含 \ $ 引号 反引号 展开等痕迹的一律 ask（合法 ref 名不可能含这些字符，误伤只落在无意义引号的罕见写法，代价一次确认）。过程自抓两个 shell 解析事实：case 的 pattern 来自变量时支持 * glob 但不解析 | 交替与 [...] 字符类（is_default_spec 的 */main 能中、pure 首版 [!...] 全不中皆因此）；字面 pattern 里 \[ 与 [:alnum:] 相邻会破坏类解析。验证：套件扩到 78 场景全绿（十六进制/八进制转义、引号即杂、@{u}、纯字面放行不回归）；\x6d 实弹 ask；bash -n、门禁通过。已回复线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 06:48：第 0 步——PR #16 收割（17 形态实弹全 ask）。本次：验证零验证历史的 md2view（自带测试套件从未被跑过）。结果：unittest 27/27 通过；真实输入全管线冒烟（README.md → parse_blocks 27 blocks → 模型创作右栏 → build_reader 产出 53KB reader.html）走通；溯源守卫双向实证——我冒烟时两次被拒（12 个 block 未引用 → 拒产；b021 无共享词锚 → 拒产），作为真实用户体验了它的门，另确认 verify_anchors 会拒绝 display:none 垫字。契约核对：parse_blocks/build_reader/verify_anchors 三个 CLI 与 SKILL.md 文档一致，无漂移。结论：md2view 零缺陷、无需改动，验证记录即本 MR 的产出；node 视觉环（measure.js/shot.js）需浏览器环境，未在本次覆盖，记为遗留。下一步：等 ci-review 审查（纯 DO.md 记录按规范直接 pass）。
- 2026-09-04 07:18：第 0 步——PR #17 收割。红队日检（09-04 首轮）：今天 13 轮全是元工作、集合零新增 skill，裁决目的仍成立（元工作直接服务装上就能用且有实获），天平提示记入日志——后续几轮应考虑真 skill。本次：验证 repo-tidy 的 SessionStart hook（git-repo-status.sh，28 行、每 session 开局基建）。新写 test_git_repo_status.sh 11 场景矩阵，坐实 2 个 hook 缺陷：①trunk 流派/无远端仓库上归位建议恒真弹出（branch != master 在 master 空串时永真，每次开局骚扰用户）；②空仓库分支名显示 HEAD（rev-parse --abbrev-ref 在 unborn 分支退化）。修复：advisory 加 -n master 前置条件；分支名改 symbolic-ref --short 优先（unborn 可读）+ rev-parse 兜底（detached）。过程自抓三件事：测试辅助函数第二次犯'路径混进 git 子命令'同类错（必须模板化辅助函数）；断言协议缺空输出/否定形态；behind 场景需先 push 再回退（场景设计错）。验证：11/11 场景全绿（含 trunk/无远端不弹建议、空仓库分支可读、detached/worktree 正常）；真实输出目检（ahead 1 + 工作区 1 的行格式正确）；bash -n、门禁通过。下一步：等 ci-review 审查。
- 2026-09-04 07:48：验证 repo-map（prompt_hook 挂在用户每次输入上，全仓库使用频率最高产物）。新写 test_repo_map.sh：scratch HOME 完全隔离（绝不触碰用户真实 ~/.claude），14 场景覆盖 scan 角色推断四态、list/resolve、prompt_hook 注入/cwd 自身跳过/词边界（kube≠kubernetes、beta≠beta-v2）/无缓存坏输入静默、增量自愈。结果：13/14 首跑即绿，唯一失败是测试对 decide_role 首中即停规则的理解错（gamma 有本人 commit 先中自研·可写，'本地·可写'需无 remote 且无本人 commit 的仓库）——非代码缺陷；顺手把 docstring 的规则顺序写显式（我这轮就被它误导）。两个 shell/bash 认知入账：BSD grep 断言协议换版时调用处未同步（-v vs !）；case 变量 pattern 规则再确认。零代码缺陷。验证：14/14 全绿；bash -n；门禁通过。遗留：schedule 子命令（launchd 注册）涉及系统状态未测；resolve 的 role 输出格式已由断言覆盖。下一步：等 ci-review 审查。
- 2026-09-04 08:16：第 0 步——ci-review 判 fail，2 条认可且是高价值批评：套件断言只验存在不验映射（角色对调变异 14/14 漏网；BOUNDARY_R 删 - 变异全绿漏网）。修复：①角色断言从缓存级存在性升级为逐仓库钉死映射（5 仓库 resolve 各断言其角色，改用 (角色 宽匹配——resolve 输出带'最近活跃'后缀）；②beta-v2 场景补'不误注入 beta 条目'反向断言。**变异验证**：机器人同款两变异复跑——角色对调被套件拦截✓、边界删 - 被套件拦截✓，恢复后 19 断言全绿。过程自抓：$var 后跟全角字符被 bash 吃进变量名第三次发生（已全文件扫描确认无同类残留）；assert 双重计数修一次。下一步：等下一轮复审。
- 2026-09-04 08:48：兑现红队天平提示，新 skill hook-test-kit：为 Claude Code hook 脚本生成行为矩阵测试骨架（scratch fixture、stdin 喂 JSON、EMPTY/!否定协议、三类已踩坑预修：辅助函数路径混入、$var 后跟全角字符、case 变量 pattern 不解析交替），SKILL.md 含变异实验收尾纪律。已注册三处索引（门禁通过：13 skill/14 entry）。狗粮验证：给 repo-tidy hook 生成 → 填 2 场景跑绿 → 变异实验（注释 echo 必红、恢复复绿）。狗粮第一口就抓到并修掉真 bug（输出目录不存在未 mkdir）。dogfood 中两次场景填写踩到 skill 自己警告的坑（引号配平、非 git cwd），反向验证了 SKILL.md 警示的必要性。下一步：等 ci-review 审查。
- 2026-09-04 09:17：验证 hkr-render 的 format.py（md → 微信内联样式 HTML，发文前每次都跑的函数；publish.py 需微信 API 不在本地覆盖范围）。方法：example 复制为本地 config（output_dir/auto_open_browser 指向 /tmp 并关闭，config.json 已被 gitignore 确认不会误提交），样例 md 过全部 7 主题。结果：7/7 主题全部产出 rc=0 的 preview.html——内联样式 10 处、标题/strong/code 命中、无外链样式表（微信合规）。过程两次修正我自己的断言：①产物路径在 <out>/<标题>/ 子目录；②link=0 是设计而非缺陷——format.py 故意把外链转成脚注纯文本（微信不允许外链），产物中 [1] 链接: URL 即正确行为。结论：format.py 零缺陷。遗留：publish.py（需微信 API）、封面生成（需图像 API）未覆盖。下一步：等 ci-review 审查。
- 2026-09-04 09:48：展示面补全——卡片墙从 9 张扩到 14 张（此前 5 个 skill 无展示卡：md2view/hook-test-kit/hkr-render/doc-reader/gpt-image2-prompt-director）。cards.json 补 5 条真实内容条目（input/output 取自实测行为，非宣传语），build_cards.py 全量重建 HTML，Playwright 逐卡截图（初版 2x 与老卡 1200x675 不一致，重截 1x 对齐），README 画廊补 5 格。验证：14 张 PNG 齐且规格一致（sips 1200x675）；md2view、hook-test-kit 两张目检（标题/标签胶囊/双栏/绿色标记渲染正常）；市场门禁通过。下一步：等 ci-review 审查。
- 2026-09-04 10:16：修正上轮画廊编辑事故（替换而非追加，吞掉 ci-review 卡与飞轮格——ci-review 判 fail 成立）。本轮在 origin/do/main 基线上重做：从 main 版 README 重放只增不删的确定性插入，结构自检过（14 张卡各恰好一次、无重复无缺失、ci-review 与飞轮格保留、tr 配平）。过程自抓：本地分支曾重建自 origin/main，与远端 do/main 分叉导致推拒——对齐基线后在其上重放编辑。教训：'追加 N 格'类编辑必须带结构断言（引用计数/配平/重复检测）；分支重建时注意 origin/main 与 origin/do/main 的分叉。下一步：等 ci-review 复审。
- 2026-09-04 10:46：ci-review 两条线程一真一旧：①ci-review 卡消失/双 tr——上一 commit 已修（线程对坏版本开出，已过时）；②md2view/hkr-render/doc-reader/gpt-image2 在画廊各出现两次（老 demo 格 + 新卡片格）——真问题且当前仍在，我的查重只数 cards/*.png 文件名没数 href 去重（第二次在画廊表上被断言盲区咬）。修复：整表程序化重建——单一事实源列表（14 卡+飞轮格，2/行），断言：href 无重复、14 张 cards/*.png 各一次、飞轮格保留、tr=8 行配平、门禁过。教训升级：同一张表改三轮后，手补/追加都不可靠，唯有'单一事实源+程序化重建+结构断言'可交付；查重维度必须对齐用户可见语义（href/skill 链接），不能只看资产文件名。下一步：等 ci-review 复审。
- 2026-09-04 11:22：验证 harness 两脚本（inspector/worker-reviewer，AI 自治循环的组装层）。新写 test_harness.sh：PATH 桩替身隔离真实 claude 调用（记录参数到 mock 日志，可选拦截动作），12 场景覆盖项目目录解析/文档注入/额外指令/消费语义/无任务不调用/完成链路调用次数/被拒绝重领。实测修复 1 个真实缺陷：/tmp/harness-context.txt 只写读不删，用户设一次'本轮额外指令'会静默泄漏进之后所有无关轮次（变量名'本轮'即 consume-once 语义）——两脚本读后即删。**两次归因修正（证据优先）**：①'\\s 是 GNU 扩展本机不认'撤回——实验证明两种 grep 均支持，POSIX 字符类改写仅作可移植性加固保留；②变异实验两次'未拦截'实为变异本身未生效（替换目标字符串写错，无操作）——'变异必须断言已生效'是我从 README 事故学过却没迁移应用的纪律，现已固化：无生效断言的变异实验无效。套件强度最终证明：删 [被拒绝] 支持的落地变异 → 套件红；恢复 → 12/12 绿。测试基建自身又修 2 处：run_wr 未引号展开分词导致 MOCK_MARK 只传了 '-'；路径断言需 pwd -P 解析符号链接。下一步：等 ci-review 审查。
- 2026-09-04 11:49：ci-review 两条全认可——consume-once 的消费边界取错：①'本轮'=一次 /harness 调用（跨 inspector+worker-reviewer 两脚本），inspector 读完就删使 full 模式下 Worker/Reviewer 再也收不到额外指令（我的改动引入的回归）；②rm 在脚本顶部而无可做任务早退在其后，无投递也无条件销毁。重新设计：inspector 永不消费（轮首读者）；worker-reviewer 用 DELIVERED 标记，仅在本次进程实际调用过 claude 后的脚本收尾删除——无可做任务早退原样保留。SKILL.md 补生命周期说明。测试套件更新为正确语义 13 场景：inspector 不消费、无可做任务不消费（回归钉）、投递后收尾消费。端到端 full 模式实弹：写上下文→inspector（保留）→worker+reviewer（2/2 可见→收尾消费）。过程自抓：套件自身 reset 清理把文件删了算到脚本头上（场景前置缺失），已修。下一步：等下一轮复审。
- 2026-09-04 12:17：第十轮复审 fail，认可：DELIVERED=1 在 claude 调用之后，而 set -euo pipefail 下 CLI 非零退出（网络/auth/配额/被杀）当场终止脚本——收尾清理整段不可达，上下文不消费，下次 /harness 复用陈旧上下文（上轮 inspector 误消费缺陷的另一入口）。修复两处：①DELIVERED=1 前移到 claude 调用之前——prompt 作为参数递出即投递事实，CLI 随后崩没崩不影响；②收尾清理改 trap EXIT——覆盖 set -e 中断/正常退出/--loop break 全部路径。测试套件 13→14 场景：新增 claude 异常退出场景（非零退出 + 上下文已消费双断言），shim 加 MOCK_FAIL 退出模式。验证：14/14 全绿；bash -n、门禁通过。已回复线程并 resolve。下一步：等下一轮复审。
- 2026-09-04 12:47：全量回归 sweep——今天 11 个 PR（含用户 #24 PR 标题自动化）合并后的集成态验证。在 origin/main 干净检出上跑全部 5 套件：git-push-guard 78/78、repo-tidy 11/11、repo-map 19/19（断言强化后逐仓库映射形态）、harness 14/14、md2view 单测 27 OK，市场门禁 13 skill/14 entry 通过。结论：当日收割整体自洽，无合并回归。过程注意：主仓检出停在用户的 task/do-pr-titles 分支，sweep 改用临时 detached worktree 跑、跑完即清，不占用户现场。下一步：等 ci-review 审查本记录；本地可测池已尽，后续方向（外部依赖类 skill 验证 / 新 skill）留给下轮结合用户反馈判断。
- 2026-09-04 13:48：ci-review 3 条实现层缺陷全认可全修复：①tagline/name 进 HTML 前转义（</td><td> 注入被中和为字面文本，td 数不变）；②render 前验 skill 目录存在（死链拒绝生成，且堵住'先写坏 README 再 --check 自愈'的陷阱；守卫只验目录存在——git-push-guard 等 hook-only 本就没有 SKILL.md）；③门禁透传 build_gallery 真实报错的最后一个非空行（此前任何失败都吞成'画廊不一致'，FileNotFoundError/缺 tagline 长一个样）。验证：三变异各带生效断言——注入转义中和（td 数不变）、死链拒生成、cards.json 移走后门禁报出 FileNotFoundError 根因；零差异/幂等/--check/恢复复绿全保持。过程自抓：路径层级错（cards-src 上三层）一次、守卫条件过严一次、门禁多行报错被 grep 单行展示误导一次、一次恢复动作被 exit 1 打断需手动补——全部当场修正。下一步：等下一轮复审。
- 2026-09-04 14:15：第十二轮复审 fail，认可：render() 对缺 name 字段的条目静默落库——name='' 使守卫撞上真实存在的仓库根目录误通过，写出空 href/cards/.png 废格且 exit=0。修复：条目循环顶部补 name 非空校验，缺失即拒生成。验证：落地变异（插入无 name 条目，带生效断言）→ 报'缺 name 字段，拒绝生成空链接格'且非零退出；恢复后 --check 与门禁复绿。render() 校验面至此闭合：name 缺失/tagline 缺失/目录不存在/HTML 注入/死链五类全拒。下一步：等下一轮复审。
- 2026-09-04 14:48：验证 gpt-image2-prompt-director 的评测器（eval_prompt_director.mjs，node）。验证矩阵：--help/--init-run（40 case 脚手架）/打分报告/空目录严格模式退出 1/--gold 自评（40 case 均 68）/阈值退出码隔离验证（gold 过硬门后：60 阈值→0、90 阈值→1）。**一次归因撤回**：初测'均分 77 阈值 60 却退出 1'疑似阈值 bug——隔离后定位为我的测试输入不合法（复制的 inputs 当输出，未过硬门 hardGatePassed=false 被正确拒绝），阈值逻辑本身正确，撤回。结论：评测器零缺陷。本地可测 skill 池至此全部见底（9 个已验证、4 个需外部资源：doc-reader/wechat/x/xhs）。下一步：等 ci-review 审查记录；后续方向交用户定夺。
- 2026-09-04 15:21：验证 doc-reader 的 web_fetcher.py（抓取引擎，真实网页实测）。连抓两个真 bug（html-to-markdown 3.x 升级兼容）：①preserve_tags 传 set，3.x 要求 Sequence → TypeError；②convert() 3.x 返回 ConversionResult 对象而非 str，clean_markdown 的 re.sub 当场炸——后者被 stdout 缓冲丢失掩盖（异常终止丢未 flush 的 print），python3 -u 才见真因。均修复：Sequence + result.content。修复后真实页面全链路通：Wikipedia/Markdown → 56KB md，13 标题/3 图片引用/源链接头。另补 SKILL.md 缺失的依赖文档（html-to-markdown 从未在文档中出现，用户会无预警撞错）。验证：退出码 0 + 产物结构断言；generate_slides.py 需 Codex imagegen 记为遗留。下一步：等 ci-review 审查。
- 2026-09-04 15:47：上轮依赖说明插在表格中间（generate_slides 与 build.py 两行之间），块引用把 build.py 行挤出表格——ci-review 判 fail 成立。本轮在干净基线上把插入点移到表格结束后，并用机器人同款渲染复现法验证：markdown 渲染需 tables 扩展（裸调不含任何表格，首测方法无效），带扩展重测——表格 4 行含 build.py ✓、依赖说明在表格外 ✓、无管道符残留行 ✓。下一步：等下一轮复审。
- 2026-09-04 16:23：清扫轮：PR #28 上一轮的表格 finding 线程 resolve 未实际生效（GraphQL 查询时序误判返回空，误以为已 resolve），阻塞自动合并——补 resolve 后推本条触发复审合并。教训：'以为 resolve 了'必须以 API 复查为准（本轮复查发现 isResolved=false 才发现）。
- 2026-09-04 16:46：收尾 sweep——上轮 sweep 后又合入 #26（画廊生成器+门禁 --check）/#27/#28（doc-reader 3.x 兼容修复），在最新 origin/main（bf5b055）重跑全部套件：git-push-guard 78/78、repo-tidy 11/11、repo-map 19/19、harness 14/14、md2view 27 OK、市场门禁+画廊 --check 通过。结论：最终集成态自洽。**当日总账**：13 个 PR 收割、149+ 断言、4 个测试套件沉淀、1 个新 skill（hook-test-kit）、画廊/门禁两类结构性防回归。本地可测池已尽；剩余 4 个 skill 需外部资源（doc-reader 全链路含 Codex imagegen / x / xhs / wechat 发文链），适合有人在场时验证。下一步：等 ci-review 审查本记录；飞轮继续按 30 分钟节奏值守，等用户指令或新证据再启动新一轮方向。
- 2026-09-04 17:20：doc-reader 收尾——build.py 预览自包含修复：产物原引用 jsdelivr CDN 的 marked.min.js 做客户端渲染，大陆环境间歇不可达即白屏，与'无需 HTTP 服务'的自包含承诺冲突。修复：vendoring——marked v12.0.2（MIT，35KB）固定版本入库，build 时内嵌进 preview.html（escape_script_content 防提前闭合），marked.min.js 列入必需文件 fail-fast 清单，SKILL.md 复制清单同步。验证（真实退出码，subprocess 取代管道 rc）：缺文件场景报清单含 marked.min.js 且 exit 1；完整场景产物 54KB、无 CDN 引用、marked 内嵌、双栏内容齐。结论：doc-reader 本地可测面（web_fetcher/build.py）全部验证闭合，仅 generate_slides.py（Codex imagegen）留待有人在场。下一步：等 ci-review 审查。
- 2026-09-04 17:46：ci-review 两条全认可，且第一条是**我自己的纪律违规**：上轮 `git add doc-reader/` 目录级暂存把早期冒烟的构建产物 doc-reader/scripts/preview.html（458 行、CDN 引用）一并提交——违反'逐个路径显式添加'，形成'去 CDN PR 里含 CDN 引用文件'的自相矛盾。修复：git rm 该产物 + .gitignore 加 doc-reader/scripts/preview.html 防再犯。第二条：空白 marked.min.js 绕过两处 fail-fast（exists 只查存在、not marked_lib 只查真空串）——改 marked_lib.strip() 判空。验证：空白文件变异（1 字节换行文件，带生效断言）→ 明确报错且真实退出码 1；删产物后 git status 干净。教训升级：目录级 git add 在含构建产物的目录上必踩雷，此教训第三次出现（sed 假通过/变异未生效/本次），全部同根——'操作后必须验证实际效果'。下一步：等下一轮复审。
